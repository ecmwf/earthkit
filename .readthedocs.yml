version: 2
formats: []
build:
  os: "ubuntu-24.04"
  tools:
    python: "3.11"
  jobs:
    post_checkout:
    - git fetch --unshallow || true
python:
  install:
  - requirements: docs/requirements.txt
sphinx:
  configuration: docs/conf.py
